export interface CreateOrderStatusDTO {
   name: string;
   isActive: boolean;
   image?:string;
  }
  
  export interface UpdateOrderStatusDTO {
    name: string;
    isActive: boolean;
    image?:string;
  }
  
 
  